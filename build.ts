// check for initial build
    // generate if it's not there
// iterate over json file
    // check length of label and makes sure it all works
        // (CFBundleName <= 15 chars, CFBundleIdentifier has to be A-Z a-z 0-9, hyphens and periods)
    // copy the initial build to new app directory
    // modify the plist as needed (CFBundleIdentifier and CFBundleName)
    // generate a new iconset (using the deno script)
        // turn iconset into an icns file and swap it in
